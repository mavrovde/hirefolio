# Environment variables
.env
.env.local

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.coverage
htmlcov/
.venv/
# ...and a bare `venv` symlink (agents symlink the toolchain into worktrees);
# the directory-only rule above does not match a symlink.
venv
env/
venv/
ENV/

# Distribution / packaging
dist/
build/
*.egg-info/

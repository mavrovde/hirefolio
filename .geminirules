# This project uses Claude Code as its primary AI tool.
#
# All AI-assistant guidance now lives in a single canonical file: CLAUDE.md
# Please read ./CLAUDE.md for stack facts, commands, the LinkedIn pipeline,
# engineering rules, and the Claude Code tooling (MCP servers, subagents,
# plugins, slash commands) configured for this repository.

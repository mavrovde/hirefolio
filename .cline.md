# Cline / RooCode Workspace Context

You are operating within the `mavrov.de` repository. Your primary goal is to provide production-ready solutions conforming to rigorous testing and formatting standards. 

## Architectural Boundaries
- **Backend**: FastAPI (Python 3.12). 
  - *Constraint*: You must format Python code with `ruff format .` before committing. Pipeline utilizes strict Ruff CI checks.
  - *Data layer*: PostgreSQL leveraging `pgvector` for AI embeddings and SQLAlchemy (async).
- **Frontend**: Angular 18 with SSR.
  - *Constraint*: Avoid raw DOM manipulation. Rely on the Angular router, signals, and dynamic bindings. Ensure code does not crash the Node.js SSR runtime when assessing APIs.
  - *Test layer*: Vitest. Coverage must be 100% for passing.
- **E2E Tests**: Playwright.
  - *Constraint*: Remember to introduce slight asynchronous waits (`waitForTimeout`) during SPA transitions to prevent flaky tests resulting from Angular rendering latency.

## Instructions for Gemini / Cloud Models
When operating as Gemini (or other top-tier models), prioritize:
1. Emitting clean, minimalistic, type-safe code that avoids legacy JavaScript/Python hacks.
2. Executing `pytest` / `vitest` immediately to verify logic mathematically before concluding tasks.
3. Keeping UI updates aesthetically pleasing using pre-existing Tailwind utility patterns.

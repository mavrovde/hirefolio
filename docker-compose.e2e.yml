services:
  backend:
    build: ./backend

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile

  admin-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.admin

  proxy:
    build: ./proxy
